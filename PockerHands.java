public class PockerHands {
















/*
if (validRank[j].equals(firstDigit) && j==8){
        ranks[i] = 10;
        rankCount[j] += 1;
        break;
    }else if(validRank[j].equals(firstDigit) && j==9){
        ranks[i] = 11;
        rankCount[j] +=1;
        break;
    }else if(validRank[j].equals(firstDigit) && j==10){
        ranks[i] = 12;
        rankCount[j] +=1;
        break;
    }else if(validRank[j].equals(firstDigit) && j==11){
        ranks[i] = 13;
        rankCount[j] +=1;
        break;
    }else if(validRank[j].equals(firstDigit) && j==12) {
        ranks[i] = 14;
        rankCount[j] +=1;
        break;
    }else if(!validRank[j].equals(firstDigit)){
        continue;  //如果是一個I 在跑完validRank都沒有匹配就會跳出迴圈了
    }else{//只有在跑完validrndk前有true匹配到才會跑這格
        ranks[i] = Integer.parseInt(firstDigit);
        rankCount[Integer.parseInt(firstDigit)-2] +=1;
        //System.out.println("founded & inserted");
        break;
    }


 */
}
